using UnityEngine;

namespace Pan.Event
{
    internal static class PanEventCreateAssetMenuInfo
    {
        internal const string PATH = "판 이벤트설정 Scriptable 생성";

        internal const string GENERAL_SETTING_SBJECT = PATH + "/통합 설정 Scriptable 생성하기";
        internal const string EVENT_INITIALIZESBJECT = PATH + "/PanEvent 초기화 설정 Scriptable 생성하기";
        internal const string EVENTVALUE_INITIALIZESBJECT = PATH + "/PanEventValue 초기화 설정 Scriptable 생성하기";
    }
}